# A股历史绩效规则机制升级

本版将历史绩效规则从“无条件硬约束”改为“历史参考 + 当前市场确认”。

## 规则原则
- 历史板块胜率/亏损只能证明过去，不能单独触发今日永久/硬性禁令。
- `SECTOR_AVOID` / `SECTOR_ADJUST` 中包含“完全禁止/熔断/完全暂停”等硬性措辞的规则，若没有 `current_confirmation.status=CONFIRMED`，只进入历史参考，不进入今日执行规则。
- 只有当前状态确认后，板块规则才可作为当日限制；确认信号应包括日/周/月趋势、资金流、相对强弱、事件催化等。
- 规则仍受 `expires_after_trades` 限制。
- 历史规则与当前规则在 Scan Prompt 中明确分层，避免旧规则压过今日行情。

## 代码文件
- `scan.py`：历史规则加载与执行层改为动态确认机制。
- `evolve.py`：进化引擎要求对板块规则提供 `current_confirmation`，禁止仅凭历史绩效生成永久熔断。
- `evolved_rules.json`：现有半导体“熔断/强制限制”已降级为历史参考，不再自动执行。
